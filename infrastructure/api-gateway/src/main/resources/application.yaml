server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    consul:
      host: localhost
      port: 8500

    gateway:
      discovery:
        locator:
          enabled: true
