AUTH_SECRET=

AUTH_URL=

KEYCLOAK_CLIENT_ID=
KEYCLOAK_CLIENT_SECRET=

KEYCLOAK_ISSUER=

API_GATEWAY_URL=

# Exposed to the browser — used by the WebSocket/STOMP chat client.
# Must be the HTTP(S) base URL of the API gateway (ws:// is derived from it).
NEXT_PUBLIC_API_GATEWAY_URL=http://localhost:8080